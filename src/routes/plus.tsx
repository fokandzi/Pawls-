import { AppHeader, AppFooter } from "../lib/app-header";
import { createFileRoute, Link } from "@tanstack/react-router";
import { seoHead, SEO } from "../lib/seo";
export const Route=createFileRoute("/plus")({head:()=>seoHead(SEO.plus),component:PlusPage});
const features=["Unlimited swipes","Advanced filters (temperament, energy, size)","Priority booking slot","Verified profile badge","See who liked you","Early access to new features"];
function PlusPage(){return <div className="flex min-h-dvh flex-col"><AppHeader active="plus"/><section className="bg-gradient-to-b from-[var(--pawls-cream-50)] to-white px-6 pb-20 pt-20"><div className="mx-auto max-w-xl text-center"><span className="rounded-full bg-[var(--pawls-cream-100)] px-4 py-2 text-sm font-semibold">Premium Experience</span><h1 className="mt-6 text-4xl font-extrabold text-gray-900">Unlock the <span className="text-[var(--pawls-terracotta-500)]">Pawls Experience</span></h1><p className="mt-4 text-lg text-gray-600">Unlimited swipes, advanced matching, priority booking, and more.</p><div className="mt-10 rounded-2xl border-2 border-[var(--pawls-gold-400)] bg-white p-8 text-left shadow-lg"><h2 className="text-2xl font-bold text-[var(--pawls-terracotta-500)]">Pawls Plus</h2><p className="mt-2 text-gray-600">Everything you need for the ultimate dog-parent experience.</p><p className="mt-4"><span className="text-4xl font-extrabold">€8</span><span className="text-gray-500">/month</span></p><ul className="mt-6 space-y-3">{features.map(f=><li key={f} className="text-sm font-medium text-gray-800">✓ {f}</li>)}</ul><a href="https://buy.stripe.com/eVqfZj1LkexGh0B80d9MY00" className="mt-8 block w-full rounded-full bg-[var(--pawls-terracotta-500)] px-6 py-3.5 text-center font-semibold text-white">Get Pawls Plus</a><a href="/plus/success" className="mt-3 block text-center text-sm text-gray-500 underline">Restore Purchases</a></div><Link to="/" className="mt-8 block text-sm text-gray-400">Maybe later →</Link></div></section><AppFooter/></div>}
